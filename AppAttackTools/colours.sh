#!/bin/bash
# colours.sh — kept for backward compatibility.
# All colour variables are now defined in config.sh; source that file instead.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/config.sh"
